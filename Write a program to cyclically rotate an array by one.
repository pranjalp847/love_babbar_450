void rotate(int arr[], int n)
{
    vector<int> v;
    int x;
    
    x = arr[n-1];
    v.push_back(x);
    for(int i=0;i<n-1;i++)
    {
        v.push_back(arr[i]);
    }
    
    for(int i=0;i<v.size();i++)
    {
        arr[i]=v[i];
    }
   
}
